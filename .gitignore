# ESP-IDF built binaries and build-related directories
build/
build_*/
sdkconfig.old
sdkconfig.defaults

# idf-ci build run output (if using idf-ci)
build_summary_*.xml
app_info_*.txt
size_info_*.txt

# pytest-embedded log folder (if using pytest-embedded)
pytest_embedded_log/

# Python-related temporary files and caches
.pytest_cache/
__pycache__/

# VS Code specific files (optional, depending on preference)
.vscode/

# Arquivo de configuração do Supabase (contém credenciais sensíveis)
supabase_config.h
managed_components/